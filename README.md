# sales_prediction_ml
This project performs an end-to-end sales data analysis and prediction workflow.   It starts with data exploration (EDA), followed by data preprocessing, model training and tuning, and finally sales prediction on a new dataset using the best-performing model (XGBoost).
